# HanziProject Development

Activate virtual environment:
.venv\Scripts\Activate.ps1

Run Flask:
py -m flask --app app run --debug

Stop Flask:
Ctrl + C

Install dependencies:
py -m pip install -r requirements.txt